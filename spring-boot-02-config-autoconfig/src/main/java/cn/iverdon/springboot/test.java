package cn.iverdon.springboot;

public class test {

    public Integer BuildTower(Integer sum, Integer target, Integer[] height) {
        if (height)
    }

    public boolean isTarget(Integer target, Integer[] height) {
        Integer[] h = new Integer[height.length];
        for (int i = 0; i < height.length; i++) {
            if (height[i] == height[i + 1]) {
                h[i] = height[i];
                h[i + 1] = height[i + 1];
                return
            }
        }
    }
}
